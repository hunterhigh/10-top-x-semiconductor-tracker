# 交接包验证记录

验证日期：2026-07-17

- 聚合规则测试：7 项全部通过，包含新增的所选窗口人物统计测试。
- 定稿 HTML：检测到 1 个内嵌 JavaScript 块，语法检查通过。
- JSON Schema：JSON 解析通过，Draft 2020-12 标识正确，全部本地 `$ref` 均可解析。
- HTML 一致性：交接包 HTML 与产品定稿源文件 SHA256 完全一致。
- 股票下钻默认值：内嵌下钻页的激活标签和初始化函数均为“今日/day”；未残留默认 `month` 初始化。
- 股票下钻概览：四项窗口指标使用统一的水平居中 `.metric` 样式，三个时间窗口共享该样式。
- 股票下钻共享窗口：今日、7日、28日切换会同步触发概览、人物立场和人物表重渲染；人物明细标题行右侧的涨跌幅随同一窗口数据更新。
- 图表悬浮层：宽度受视口限制，左右边缘事件分别向页面内部展开，避免缩窄页面时被裁切。
- 最近观点语义：人物表读取最新一条结构化记录方向，不再把累计“多空均有”状态显示为最近观点。
- 共享窗口布局：84px/11px小尺寸分段按钮、人物比例图、数字概览、人物明细按最终顺序渲染。
- 人物表 neutral 样式：无方向比例段和文字使用浅灰弱化，不影响统计口径。
- 时间控制区：三个时间按钮位于“10人观点统计”同一行最右侧；唯一窗口时间标签位于“人物明细”标题右侧，并由同一窗口状态同步更新。
- 比例图层级：整体人物分布保留24px主条；个人构成条按最终确认恢复18px及饱和多空色，neutral 保持浅灰。
- 信息层级：整体人物立场使用主摘要卡，四项数字使用次级指标卡，人物表由独立“人物明细”标题引入。
- 细节样式：时间范围使用13px浅蓝标签，立场标题增加左缩进；个人比例条恢复18px粗条及饱和多空色，neutral 保持浅灰。
- 最终控制层级：时间按钮缩为84px/11px；仅保留人物明细上方一个窗口时间标签；人物明细黑色底边已删除。
- 人物明细时间标签：位于标题右侧并保留28px组内间距，不再占据标题栏最右端。
- 标题与涨跌布局：小尺寸窗口按钮位于“10人观点统计”右侧；涨跌幅和价格区间位于人物明细标题行最右侧。
- 时间按钮对齐：桌面端位于“10人观点统计”同一行最右侧；窄屏清除自动边距并换至下一行。
- 单文件边界：交付 UI 文件为单 HTML；头像和股票下钻均已内嵌，生产实现不得恢复本地 assets 或独立 stock-detail.html 依赖。

定稿 HTML SHA256：

`F20FE96614FAD70B14C28CFBA397566039B0BDC42E6D9FD1847BAE47B75AF446`

说明：本次环境未安装第三方 `jsonschema` 包，因此 schema 验证采用标准 JSON 解析和本地引用完整性检查。接入后端 CI 时，建议使用 Draft 2020-12 兼容验证器对实际 render payload 做完整实例校验。
